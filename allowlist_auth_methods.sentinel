# allowlist for the following auth methods
# oidc
# token
# approle

import "strings"

auth_allowlist = ["auth/oidc","auth/token","auth/approle"]

auth_allowlist_check = rule {
  request.path in auth_allowlist
}

main = rule when strings.has_prefix(request.path, "auth/"){
auth_allowlist_check
}
